# KacPred

Deep learning framework for cross-species lysine acetylation site prediction.

## Project Structure

- `src/`: model, dataset, feature engineering, and training utilities
- `scripts/train_cv.py`: 5-fold cross-validation and hard voting
- `data/`: data description
- `results/`: saved outputs

## Requirements

```bash
pip install -r requirements.txt
